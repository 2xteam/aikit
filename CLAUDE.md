# 작업 전에 읽을 것

이 프로젝트의 배경 지식은 별도의 옵시디언 볼트에 정리되어 있다.

```
로컬:   C:\Dev\my-obsidian-vault
저장소: https://github.com/2xteam/my-obsidian-vault
```

## 먼저 읽기

1. `10-Projects/AIKit.md` — 이 앱의 사실(스택·데이터·결정·함정)
2. `50-Plans/H AIKit 구축.md` — **착수 순서와 남은 일.** 진행하면 여기를 갱신한다
3. `00-Meta/AI 협업 규칙.md` — 볼트를 읽고 갱신하는 방법

작업 성격에 따라 추가로:

| 작업 | 노트 |
|---|---|
| 페이지 디자인 | `20-Design/` 전체 (특히 결쩜사 페이지 패턴·카피 톤앤매너) |
| 로그인·회원·세션 | `30-Patterns/인증과 세션 공유.md` |
| 이미지 업로드 | `30-Patterns/이미지 업로드 패턴.md` |
| 이미지 저장소 | `40-Infra/Cloudflare R2.md` |
| 배포·환경 변수·도메인 | `30-Patterns/Vercel 배포 패턴.md`, `40-Infra/도메인과 DNS.md` |
| DB 연결 | `40-Infra/MongoDB Atlas.md` |
| admin | `30-Patterns/통합 admin.md` |

## 이 프로젝트 메모

- 로컬 포트 **3008** (검증 3018). 전체 포트 표는 볼트 `Home.md`
- DB 는 `aikit`. `lib/db.ts` 에서 이름을 **코드에 못 박는다.** URI 경로도
  환경 변수도 믿지 않는다 (`MONGO_DB` 같은 변수를 만들지 말 것)
- 회원은 `user` DB 공유. **API 는 쿠키의 `id` 를 믿지 않는다** — 세션의 서명 토큰을 검증한다
- 첫 메뉴는 **Prompt Log**. 상단 메뉴 이름은 `Home` `Logs`, 화면 헤드라인에서 이름을 다 쓴다
- 화면 문장은 전부 **해요체**
- ⚠️ 카피에서 **일곱 앱을 묶어 한 제품처럼 소개하지 않는다.** AIKit 만 쓰는 사람이
  정상이다 → `20-Design/서비스 카테고리와 카피 원칙.md`

## 이 앱에서 가장 많이 어긋나는 자리 — 이미지 공개 원칙

> **R2 객체 URL 을 밖으로 내보내지 않는다. 항상 앱 라우트가 대신 내보낸다.**

| | 누가 본다 | 어떻게 |
|---|---|---|
| 기본 | 본인만 | 앱 라우트가 세션 확인 후 스트리밍 |
| 공유 | 링크를 아는 사람 | 앱 라우트가 **공유 켜짐 + 이 이미지가 선택됨**을 확인 후 스트리밍 |

직링크를 노출하면 **"공유 끄기" 가 실제로 먹지 않는다.** 크롤러와 CDN 캐시에 남아서
되돌릴 수 없다. 프록시 라우트면 끄는 즉시 404 가 된다.

그래서 이 저장소에는 **`R2_PUBLIC_URL` 이 없다.** 다른 앱의 `lib/r2.ts` 를 복사할 때
`getR2PublicUrl()` 을 함께 들여오지 말 것.

- `r2Key` 를 API 응답에 **절대 싣지 않는다**
- 딥링크에 이미지 URL 을 싣지 않는다 (2026-09-18 접었다). 대신 원본 다운로드 버튼
- 묶음·회원을 지우면 R2 객체도 함께 지운다 (고아 객체가 용량을 먹는다)

## 디자인을 만질 때

**먼저 검사부터 돌린다.**

```bash
cd C:/Dev/myjane && npm run design:check
```

규칙 · 이유 · 현재 기준선 → 볼트 `20-Design/여섯 앱 디자인 시스템.md`

- `components/Sheet.tsx` · `ScrollProgress.tsx` · `TopNav.tsx` 는 **복사본**이다.
  여기서 고치면 일곱 벌이 갈라진다 — 고칠 때는 함께 고친다
- 아이콘은 **일곱 개가 한 가족**이다. 하나만 바꾸지 않는다
- 시트의 원형 장식은 쓰지 않는다 (2026-09-07 에 걷었다)

### 포인트 요소 넷 — `app/elements.css` 는 생성 파일이다

고치지 말 것. 원본은 포털에 하나뿐이다.

```
myjane/design/elements.css                        ← 여기만 고친다
cd C:/Dev/myjane && npm run elements -- --write    ← 일곱 앱이 함께 갱신된다
```

⚠️ `app/layout.tsx` 에서 `elements.css` 는 **globals.css 다음 줄**이라야 한다.
`@import` 로 넣으면 `.sheet--point` 가 특이도 같은 `.sheet` 에 조용히 진다.

## 색을 바꿀 때

**`app/palette.css` 를 직접 고치지 말 것.** 생성 파일이다.

```
myjane/design/palette.json                       ← 여기만 고친다
cd C:/Dev/myjane && npm run palette -- --write    ← 일곱 앱이 함께 갱신된다
```

⚠️ 밝은 색을 글자로 쓰지 말 것. 면적용과 글자용이 따로 있다 —
`--accent` / `--accent-ink`, `--point` / `--point-ink`, `--danger` / `--danger-ink`.

⚠️ 다른 앱 파일을 복사하면 `color: "#000"` 과 `rgba(0,0,0,...)` 그림자가 함께 넘어온다.
복사 직후 그레프한다.

## 작업이 끝나면

바뀐 사실(도메인·DB·진행 상황·새로 발견한 함정)을 볼트의 해당 노트에 반영하고
`updated` 날짜를 올린다. 볼트 수정은 코드와 **별도 커밋**으로 남긴다.

비밀값은 볼트에 쓰지 않는다. 공개 저장소다.
